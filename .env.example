# ─────────────────────────────────────────────────────────────
# 复制本文件为 .env 后按需修改： cp .env.example .env
# 本地版默认不需要任何配置即可运行。
# ─────────────────────────────────────────────────────────────

# 本地服务地址。
# 留空 = 同源（打包成 exe 后前端由 Node 服务直接托管，就是这个情况）。
# 只有开发模式下前端跑在 vite 自己的端口（5173）时才需要填：
# VITE_API_BASE=http://127.0.0.1:5178

# 可选：Sentry 前端错误上报 DSN，留空则不上报
VITE_SENTRY_DSN=

# 可选：指定 FFmpeg 可执行文件路径。
# 不填时按 ffmpeg-static → 应用目录 resources/ffmpeg/ → 系统 PATH 的顺序查找。
# DONGHUA_FFMPEG=C:/ffmpeg/bin/ffmpeg.exe
