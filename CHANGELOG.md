# 更新日志

本文件记录适合 GitHub Release / 仓库首页阅读的主要变化。版本号以 `package.json` 为准。

## 3.0.0 — 2026-09-22

本次更新的核心是把原本相对独立的创作页面继续连接成可复用数据的生产链，同时加强本地剪辑、OpenReel 集成和回归测试。

### 创作生产链

- 图片生成结果保存为素材后，可回写来源分镜的 `image_asset_id`。
- 视频生成结果保存为素材后，可回写来源分镜的 `video_asset_id`。
- 一次生成多张候选图片时，可指定其中一张为「分镜主图」。
- 分镜页支持多选、全选、批量生图和批量出片入口，并向下游传递 `storyboardQueue`。
- 分镜对白 / 旁白与音频资产继续保持绑定关系。

### 角色 / 场景 / 道具创作资产

- 不破坏原有 `AssetType`：仍以图片资产为基础，通过 `metadata.category` 标记 `character / scene / prop`。
- 素材库支持角色、场景、道具、未分类筛选。
- 通用素材选择器支持按创作资产类别筛选。
- 图片和视频提示词支持 `@素材名`。
- 输入 `@` 时可从当前项目创作资产中自动补全。
- 图片生成会把匹配的创作资产加入参考图。
- 视频生成按角色参考、场景参考、普通参考映射创作资产，并避免与手工选择的素材重复。
- 首尾帧 / keyframes 模式保持专用帧顺序，不自动混入创作资产。

### 条漫

- 增加小红书竖图、公众号长图、手机条漫、方形四格等画布预设。
- 可直接复用当前项目图片素材。
- 支持复制画布元素。
- 可从项目全部分镜或指定剧本的分镜自动生成条漫初稿。
- 已绑定图片的分镜优先使用图片；没有图片时保留画面描述占位。
- 对白 / 旁白自动生成气泡或旁白块。
- 根据分镜数量自动扩展长图高度。

> 条漫真实长图渲染导出仍在完善，本版本没有把占位导出描述成已完成能力。

### OpenReel 与剪辑

- 新增 OpenReel Desktop 本地发现、启动和 MCP 连接基础。
- 支持读取 OpenReel 本地 MCP endpoint、列出工具、调用工具。
- 支持创建 OpenReel 项目、导入媒体和从本地时间线构建 OpenReel 时间线计划。
- 本地大文件支持受控暂存、进度、ETA、取消和终态恢复。
- OpenReel Web 与本项目简易 FFmpeg 剪辑台继续作为回退。
- 加强剪辑参数校验：sourceStart、speed、volume、trim、transition、muted、reversed。
- 媒体路径统一做目录边界检查，修复路径穿越和 Range 解析边界问题。
- 修复数据层写锁长期残留、项目删除后的 stale selection、对象 URL 清理等问题。

### 工程与测试

- 本地测试可通过 `DONGHUA_DATA_DIR` 使用隔离数据目录。
- API smoke 使用 Node 实现，减少额外运行时依赖。
- 当前回归测试覆盖：
  - 42 项核心逻辑 / 数据层 / FFmpeg 命令测试
  - 7 项 FFmpeg 渲染 E2E
  - 3 项渲染精度场景
  - 11 项 HTTP / API smoke
- TypeScript typecheck 与 Production Build 纳入发布前验证。

### 当前已知未完成项

- 分镜批量生图 / 批量出片已建立队列入口，但下游自动连续消费整个队列尚未完成。
- 条漫长图真实渲染导出尚未完成。
- OpenReel Desktop 的普通前端提交路径仍有进一步完善幂等 requestId 和失败片段恢复的空间。

